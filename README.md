# WebframeworkDjangoVuePostgres
A basic web framework package as a starting point for platform ideas with docker support.

# Start routine:
1. cd into local_infrastructure
2. use the makefile to start the docker containers
